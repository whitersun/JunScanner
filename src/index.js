class JunScanner {
    constructor(selector, opts) {
        const supportScanTypes = []

        const defaults = {
            fps: 10,
            scanArea: {
                width: 250,
                height: 250
            },
        }
    }
}